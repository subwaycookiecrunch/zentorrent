# zentorrent
fast + clean cli torrent downloader in go. zero bs.

```sh
git clone https://github.com/yourusername/zentorrent
cd zentorrent
go build -o zentorrent
```

usage:
`zentorrent ubuntu.torrent`
or
`zentorrent "magnet:?xt=urn:btih:..."`

q or ctrl-c to quit

